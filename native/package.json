{
  "name": "emoji-blasters-native",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor の殻。ここだけが npm を使う —— ゲーム本体(src/)は依存ゼロ・ビルド工程ゼロのまま。",
  "type": "module",
  "scripts": {
    "web": "node copy-web.mjs",
    "sync": "node copy-web.mjs && cap sync",
    "android": "node copy-web.mjs && cap sync android && cap open android",
    "ios": "node copy-web.mjs && cap sync ios && cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.0"
  },
  "dependencies": {
    "@capacitor/core": "6.2.0",
    "@capacitor/android": "6.2.0",
    "@capacitor/ios": "6.2.0",
    "@capacitor/haptics": "6.0.2",
    "@capacitor/app": "6.0.2",
    "@capacitor/status-bar": "6.0.2",
    "@capacitor/screen-orientation": "6.0.3"
  }
}
