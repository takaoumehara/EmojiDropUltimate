# OPEN QUESTIONS — EmojiDrop Ultimate

> 作成: 2026-09-06 ／ 対象: `portfolio-case-study-emojidrop/README.md` · `MEDIA.md` · `DISTRIBUTION-FACTS.md`
> README 側にも「不明・要確認」として残していますが、**一度に答えられるようにここへ全部集めました。**
> 各項目は「どこの何か」「なぜ分からないか」「どう答えれば埋まるか」の3行です。
>
> **Q-1 / Q-2 / Q-6 / Q-11 の4つが、他より先に効きます。**

---

## A. これが無いとページが出せない

### Q-1 本番URL ★
- **どこの何か**: §1 基本情報「公開リンク」と §1「現在の状態」。索引カードからの導線、
  そして `DISTRIBUTION-FACTS.md` の「正直な但し書き」に直接効きます。
- **なぜ分からないか**: `vercel.json` はあり、`api/` の3つのサーバーレス関数に
  `maxDuration` まで設定されているので**どこかにデプロイされている可能性が高い**のですが、
  リポジトリ全体を検索して `*.vercel.app` も独自ドメインも**1件も見つかりませんでした**。
  会社サイト `creativityiseverywhere.com/playable.html` も 2026-08-19 時点で
  "No public URL yet" のままです。
- **どう答えれば埋まるか**: Vercel ダッシュボードの Domains に出ている**本番URLを貼る**か、
  「まだ公開していない」の一言。**URLが無いなら「プロトタイプ」のままにします**
  （推測のURLは絶対に載せません）。

### Q-2 名前は「EmojiDrop Ultimate」か「Emoji Blast」か ★ — **回答済み: Emoji Blast**
> 2026-09-12 に②で確定。**アプリ側（一般ユーザーに見える部分）は改名済み**です:
> ページタイトル・OGP・manifest・ホーム画面名・ロゴ・共有カード・ハッシュタグ（`#EmojiBlast`）・
> 招待文・動作レポート・保存ファイル名・プライバシーポリシーの表題。
> **未対応**: このケーススタディ一式（本ファイルを含む）、リポジトリ名、README、`docs/` の設計資料、
> takaoumehara.com と creativityiseverywhere.com のカード。以下は改名前の記述です。
- **どこの何か**: §1 プロジェクト名、§2 カード、ページのタイトル、`DISTRIBUTION-FACTS.md` 全体。
- **なぜ分からないか**: リポジトリ名・README・`manifest.webmanifest`・`sharecard.js` の
  ハッシュタグ（`#EmojiDropUltimate`）・takaoumehara.com のカード・
  creativityiseverywhere.com のカード、**書かれているものは全部「EmojiDrop Ultimate」/
  「EmojiDrop」**です。一方、会話では「Emoji Blast」と呼ばれています。
  **こちらでは一切改名していません**（指示どおり、リポジトリの名前で書いてあります）。
- **どう答えれば埋まるか**: ①「EmojiDrop Ultimate のままで良い」②「Emoji Blast に統一したい」
  のどちらか。②の場合、リポジトリ名・README・manifest・ハッシュタグ・既存の2サイトのカードまで
  波及するので、**改名は別作業として切り出す**ことを推します。

### Q-6 使える機材 ★
- **どこの何か**: `MEDIA.md` の撮影指示 T-1〜T-5（道具と手順が確定しません）。
  この案件は**2台のスマホの同時収録**が主役なので、ここが一番効きます。
- **なぜ分からないか**: 手元の資料に機材の記載が無いため、Mac + iPhone2台 + iMovie を
  仮に置いています。
- **どう答えれば埋まるか**: ①PC は Mac / Windows ②**遊べるスマホは何台あるか**（2台要ります）
  ③iPhone / Android の別 ④三脚の有無 ⑤編集ソフト（iMovie / Premiere / DaVinci / CapCut / 無し）
  の5点を並べて回答してください。

### Q-11 ② の生成に着手してよいか ★
- **どこの何か**: `MEDIA.md` §2「② の確認待ちリスト」。図8点＋実スクリーンショット。
- **なぜ分からないか**: 不要なものまで作るとコストになるだけなので、着手前の確認待ちにしています。
  **この案件では1点も作っていません**（実画面が無いので、サムネイルすら作れていません → Q-7）。
- **どう答えれば埋まるか**: 「B の図だけお願い」「1〜3だけお願い」のように**番号で**指定してください。

---

## B. 事実確認（答えは短くて構いません）

### Q-3 既存カードの記述を直してよいか
- **どこの何か**: `takaoumehara.com/interactive.html:754` と
  `creativityiseverywhere.com/playable.html:135`。
- **なぜ分からないか**: どちらも **"two players head-to-head"（対戦）** と書いていますが、
  実装は**協力プレイ（co-op）**です（残機はチーム共有、ボスHPは共有プール、
  「きずな」は2機を線で結ぶ味方どうしの技）。また interactive.html は
  "no server relay either" と書いていますが、いまは `server/` に**任意の中継サーバーが
  実装済み**（既定オフ）です。直すかどうかはサイト側の判断です。
- **どう答えれば埋まるか**: 「直して」または「今回は触らない」。
  直す場合の文言案は `DISTRIBUTION-FACTS.md` の1文説明をそのまま使えます。

### Q-4 着手日はいつか
- **どこの何か**: §1「制作年・期間」。
- **なぜ分からないか**: `git log` の最初のコミットは **2026-07-28** ですが、その1コミット目に
  既に README・章・集中モード・PWA・`api/` 3本が入っています（28ファイル）。
  **その前に別の場所で作っていた期間があるはず**ですが、履歴には残っていません。
- **どう答えれば埋まるか**: 「◯月◯日ごろ着手」の一言。分からなければ
  「2026-07（リポジトリ作成は 2026-07-28）」と書きます。

### Q-5 入力ラベルは `Two phones` で良いか
- **どこの何か**: §1「入力ラベル」。Interactive Experience の索引で全カードに並ぶラベルです。
- **なぜ分からないか**: ソロでも完全に遊べる作品なので、1語に潰すとどちらかが落ちます。
  現在は **`Two phones`** にしました（既存カードと一致、かつ「きずな」は1人では
  構造的に成立しないため）。対抗は `Drag`（ソロの実際の操作）。
- **どう答えれば埋まるか**: 「`Two phones` でOK」または「`Drag` に変える」の一言。

### Q-7 サムネイル（760×570）をどうするか ★
- **どこの何か**: `MEDIA.md`。**これが無いと索引ページにカードを置けません。**
- **なぜ分からないか**: リポジトリに**プレイ画面の画像が1枚も無い**ので、実画面から
  切り出せません。`og.png`（1200×630）はありますが**比率が違い**（16:9 相当 → 4:3 が必要）、
  中身が実際のプレイ画面かどうかも私には確認できません（画像を開いて見ていないため）。
- **どう答えれば埋まるか**: ①「`og.png` を見て、実画面なら4:3にトリミングして使う」
  ②「T-1 のスクリーンショットを撮るので、それを使う」③「代替を作ってもらう」のどれか。
  **②が一番良い**です（実画面が使えます）。

### Q-8 アクセント色（`resona` と重複します）
- **どこの何か**: §12。
- **なぜ分からないか**: 指定表の Interactive = **ティール `#0d9488`** を提案しましたが、
  `resona` のケーススタディも同じティールを提案しています。同じ索引で隣接すると、
  「1プロジェクト1色」の意味が薄れます。
- **どう答えれば埋まるか**: 「ティールでOK」「こちらを紫 `#7c3aed` に」「おまかせ」のどれか。

### Q-9 リポジトリは公開しているか
- **どこの何か**: §1「公開リンク」。ケーススタディからリポジトリへリンクを張るかどうか。
- **なぜ分からないか**: この環境からは GitHub 上の公開設定を確認していないため
  （`git remote -v` で URL は分かりますが、可視性は分かりません）。
- **どう答えれば埋まるか**: 「公開」か「非公開」の一言。非公開ならリンクは外して文章だけにします。

### Q-10 「実装の大半はAIエージェントが書いた」と公開ページに書いてよいか
- **どこの何か**: §1「自分の役割」と §6.5 Craft 全体。
- **なぜ分からないか**: `git shortlog -sn` は 50コミット中48がエージェント名義で、これは事実です。
  ただし**ポートフォリオでどこまで前に出すかは戦略の問題**で、事実からは決められません。
  現在は「実装の大半はエージェント。私がやったのは判断」と正面から書いてあります。
- **どう答えれば埋まるか**: ①そのまま書く（**推します**。§6.5 の「AIが何を間違えたか」が
  このページで一番強い部分なので、前提を隠すと成立しません）②「AIエージェントを運用した」と
  だけ書いて比率は出さない ③触れない、のどれか。

---

## C. 数字と表現の詰め

### Q-12 ①②の数字を測り直すか
- **どこの何か**: §9 Proof の ①`0.29秒` と ②`±1%`。
- **なぜ分からないか**: **この作業ではテストを実行していません**（読む・書くだけの指示のため）。
  両方ともリポジトリの文書に「実測」として記録された値で、確認日はその**記録日**
  （2026-07-29 / 2026-08-19）です。その後もコードは変わっています。
- **どう答えれば埋まるか**: ①「記録日つきでそのまま載せる」（現在の方針）
  ②「`node --test test/*.test.js` を走らせて 2026-09-06 の値に更新する」のどちらか。
  ②なら **5分**で終わります。

### Q-13 テストの件数を載せるか
- **どこの何か**: §9「参考として持っている実測値」の表（**現在はページに載せない方針**）。
- **なぜ分からないか**: 文書に **192件**（2026-07-29）と **201件**（2026-08-01）の2つが残っていて、
  私が今日静的に数えると **212件**（`test(` の宣言・15ファイル・実行はしていない）。
  どれも間違いではなく、時点が違うだけです。
- **どう答えれば埋まるか**: Q-12 で②を選ぶなら、**実行結果の件数を1つだけ**載せるのが一番きれい。
  走らせないなら、この数字はページから外したままにします。

### Q-14 README の「6体」を直すか（本体リポジトリ側の作業）
- **どこの何か**: ケーススタディ §10 Retrospective ③ で、この矛盾を**そのまま材料として**
  書いています。ただし本体の `README.md:17` は今も間違ったままです。
- **なぜ分からないか**: 直すとケーススタディの③が「直したもの」に変わり、
  直さないと README が間違ったまま公開される。どちらを取るかは本人の判断です。
- **どう答えれば埋まるか**: ①「README を直す」（③は「数週間間違っていた」という過去形で書き直します）
  ②「ケーススタディを出すまで残す」のどちらか。**①を推します**
  （③の主張は「気づいたこと」であって、「放置していること」ではないほうが強い）。

### Q-15 スライド枚数
- **どこの何か**: §0 の判定。現在 **全12枚**（Hero / Tension / Approach / Structure /
  Evidence×3 / Craft / Craft-不採用 / Proof / Retrospective / Closing）。
- **なぜ分からないか**: 判定 B の枠は 10〜13枚で、どこに寄せるかは編集判断だからです。
- **どう答えれば埋まるか**: 「12でOK」、または削りたいスライド名を挙げてください
  （削るなら **Structure の C（サーバー3種）** を Craft に1行で吸収するのが一番傷が浅いです）。

### Q-16 カテゴリタグ3つ目
- **どこの何か**: §2「カテゴリタグ」。現在 `Interactive Experience` / `Game Design` / `WebRTC`。
- **なぜ分からないか**: サイト側で既に使っているタグ語彙が手元に無いため、揃っているか
  確認できません（既存カードは `Prototype` / `Game` / `WebRTC` を使っています）。
- **どう答えれば埋まるか**: 既存タグの一覧を貼るか、「そのままでOK」の一言。
